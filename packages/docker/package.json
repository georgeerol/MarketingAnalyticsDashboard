{
  "name": "docker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "install": "echo 'Nothing to install for docker package'",
    "dev": "docker-compose up -d",
    "stop": "docker-compose down",
    "clean": "docker-compose down -v"
  }
}